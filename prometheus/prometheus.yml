global:
  scrape_interval: 5s # Как часто собирать метрики (для лаборатории ставим поменьше)

scrape_configs:
  - job_name: 'flask-notes-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:5001'] # Docker сам отрезолвит имя контейнера в IP