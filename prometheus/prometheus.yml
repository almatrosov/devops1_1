global:
  scrape_interval: 5s # Интервал опроса приложения (для лаборатории ставим 5 секунд)

scrape_configs:
  - job_name: 'flask-notes-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:5000'] # Docker сам сопоставит имя 'backend' с IP контейнера
